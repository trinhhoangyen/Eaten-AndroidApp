<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorOrgane</item>
        <item name="colorPrimaryDark">@color/colorOrgane</item>
        <item name="colorAccent">@color/colorOrgane</item>
    </style>

</resources>
